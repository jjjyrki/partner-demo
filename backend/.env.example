# Demo login credentials (after npm run db:seed):
#   alice   / password123  (5 open tasks)
#   bob     / password123  (1 open task)
#   charlie / password123  (3 open tasks)

DATABASE_URL=postgres://postgres:postgres@localhost:5433/task_platform
JWT_SECRET=your-secret-key-change-in-production
PORT=3000
CORS_ORIGIN=http://localhost:5173
NODE_ENV=development
